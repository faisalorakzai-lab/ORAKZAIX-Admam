{
  "version": 2,
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "cd artifacts/orakzaix && pnpm run build",
  "outputDirectory": "artifacts/orakzaix/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "VERCEL": "1"
  }
}
